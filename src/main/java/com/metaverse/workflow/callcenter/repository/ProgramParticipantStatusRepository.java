package com.metaverse.workflow.callcenter.repository;

import com.metaverse.workflow.model.ProgramParticipantStatus;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProgramParticipantStatusRepository extends JpaRepository<ProgramParticipantStatus,Long> {
}
